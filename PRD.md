# 코토다망 배포 전환: GitHub + 다운로드 사이트 + 자동 업데이트

.bat/.exe 수동 배포를 GitHub 릴리스 기반 배포로 옮기고, 전용 다운로드 사이트와 앱 내 자동 업데이트를 추가한다. 이미 있는 델타 업데이트 인프라를 뼈대로 삼는다.

| 항목 | 내용 |
|---|---|
| 대상 버전 | v1.25.1 이후 |
| 현재 상태 | .bat + 수동 배포 |
| 스크린샷 인식 | 영향 없음, 유지 |
| Supabase | 필요 없음 |

**한눈에 보기**

- 배포본 크기: 400MB → 375MB (ffmpeg 트림 수정 후)
- 캐릭터 이미지: 144MB → 70MB (팔레트화 후)
- 이번 세션에서 발견·수정한 델타 파이프라인 버그: 6건
- 필요한 신규 백엔드 서비스: 0개

---

## 1. 이미 갖고 있는 것

v1.25.1에서 만든 `DataUpdateService` 델타 업데이트 시스템이 이 계획의 핵심 인프라다. 게임 데이터(캐릭터·이미지·인식 학습 참조)는 이미 GitHub 릴리스 기반 배포에 필요한 형태를 갖췄고, 이번 세션에 실제로 끝까지 동작하는 것까지 검증했다.

- **[기존] 델타 패키지 포맷** — `manifest.json` + `characters_delta.json` + 이미지/참조 파일. 버전 체인(`FromDataVersion → DataVersion`), SHA256 검증, 3-way merge, 적용 전 자동 백업.
- **[기존] CLI 진입점** — `KotodamanWordFinder.exe --apply-data-update <zip>`. 다운로드한 델타를 앱 재시작 없이 적용하는 경로가 이미 있다.
- **[신규 필요] ①** GitHub에서 최신 버전을 확인하는 로직
- **[신규 필요] ②** 델타/앱 자체를 내려받는 로직
- **[신규 필요] ③** 앱 바이너리 교체용 Updater

---

## 2. 세 축 아키텍처

GitHub은 소스 저장소이자 배포 CDN 역할을 겸한다. 다운로드 사이트는 정적 페이지 하나면 충분하고, 서버 로직이나 DB가 필요한 지점이 없다.

```
[GitHub]                    [Vercel]                    [앱]
저장소 + Releases      →     다운로드 사이트        →     자동 업데이트
소스 코드, 버전 태그.        정적 페이지. GitHub          실행 시 최신 버전 조회 →
Release 자산으로              Releases API로 최신 버전     데이터만 바뀌면 앱 내에서
self-contained 포터블         을 표시하고 자산 링크로      델타 적용(재시작 불필요) →
ZIP과 데이터/앱 델타 ZIP       연결. 서버 로직 없음.        앱 자체가 바뀌면 Updater 경유
을 첨부.
```

---

## 3. 기능 요구사항

### FR-1 — 최초 다운로드 `P0` `설계 필요`

다운로드 사이트에서 self-contained 포터블 ZIP을 받는다. 압축 해제 후 `run.bat` 없이 exe를 바로 실행할 수 있어야 한다(현재 배포본은 이미 self-contained라 .NET 런타임 설치가 불필요).

사이트는 GitHub Releases API에서 최신 태그를 조회해 항상 최신 자산을 링크한다 — 사이트를 수동으로 갱신할 필요가 없어야 한다.

### FR-2 — 데이터 전용 업데이트 `P0` `기반 검증 완료`

앱 실행 시 GitHub에서 버전 매니페스트를 확인해 새 데이터 버전이 있으면 델타 ZIP을 내려받아 `DataUpdateService.ApplyPackage`로 적용한다. 재시작 불필요 — 지금 `--apply-data-update` CLI 경로와 같은 코드 경로를 재사용한다.

이번 세션에 캐릭터/이미지에 더해 `RecognitionReferences`(인식 학습 참조 이미지)도 같은 델타 체계에 편입했고, 실제 PowerShell 생성 ZIP을 C#으로 끝까지 적용 검증했다(§5 참고).

### FR-3 — 앱 자체 업데이트 `P0` `설계 결정 필요`

실행 중인 프로세스는 자기 자신의 DLL(`coreclr.dll`, `PresentationFramework.dll`, `OpenCvSharpExtern.dll` 등)을 교체할 수 없다. 새 버전을 임시 폴더에 내려받은 뒤, 앱 종료 → 별도 Updater 프로세스가 파일 교체 → 앱 재실행 흐름이 필요하다.

두 갈래 중 하나를 고른다: **기성 프레임워크**(예: Velopack — GitHub Releases를 델타 소스로 바로 쓰는 .NET 데스크톱 자동 업데이트 라이브러리)를 붙이거나, **자체 Updater.exe**를 배포 자산에 포함해 최소 기능만 구현한다. 전자가 검증된 코드를 재사용하는 쪽, 후자가 의존성을 안 늘리는 쪽이다.

### FR-4 — 패키지 무결성 검증 `P1` `계획`

지금 이미지·참조 파일은 SHA256으로 검증하지만 `manifest.json`·`characters_delta.json` 자체는 검증 없이 신뢰한다. 사용자가 직접 드래그&드롭하던 전제에서는 수용 가능했지만, 자동으로 네트워크에서 받아 자동 적용하면 전제가 바뀐다. 최소한 릴리스별 `latest.json`에 각 자산의 SHA256을 명시하고 다운로드 직후 대조한다.

### FR-5 — 델타 체인 정리 `P1` `계획`

여러 버전 뒤처진 사용자는 델타를 순서대로 여러 개 받아야 한다. 오래된 델타가 무한히 쌓이지 않도록 주기적으로 전체 스냅샷 릴리스를 내고, 그 이전 델타는 체인에서 접는 정책이 필요하다(예: 5개 데이터 버전마다 스냅샷).

### FR-6 — 버전 조회 캐싱 `P2` `계획`

다운로드 사이트가 서버에서 GitHub API를 매 요청마다 조회하면 Vercel 서버 IP 하나로 몰려 시간당 60회 제한에 걸릴 수 있다. 정적 생성 시점에 캐시하거나 클라이언트 사이드에서 직접 조회한다(사용자 개인 IP라 문제없음). 앱의 버전 확인도 사용자 개인 IP라 이 문제와 무관하다.

---

## 4. 비기능 요구사항

| 항목 | 현재 상태 | 필요 조치 |
|---|---|---|
| 배포본 용량 | 375 MB (트림 수정 후) | OpenCV(59MB)·.NET 런타임(160MB)은 고정비. 실제 갱신 대상은 매번 수 MB 뿐이라 델타로 충분히 커버. |
| 코드 서명 | 없음 | 서명 없으면 다운로드·업데이터 실행 시 SmartScreen 경고. 연 단위 인증서 비용 대 경고 감수 여부 결정 필요(§6). |
| 이미지 자산 | 70 MB / 2307장 | git에 직접 커밋하지 않는다. Release 자산으로만 배포하고 리포에는 소스 코드와 baseline 해시(360KB)만 둔다. |
| GitHub API 레이트리밋 | 60/시간 (미인증) | 다운로드 사이트는 정적 캐시, 앱은 사용자별 IP라 별도 조치 불필요. |

---

## 5. 이번 세션에 검증된 것

계획 수립 전에 델타 업데이트 인프라가 실제로 끝까지 동작하는지 직접 실행해 확인했다. 그 과정에서 이 계획이 의존하는 파이프라인 자체의 결함 6건을 찾아 고쳤다 — 전부 이번 요청과 무관하게 이미 있던 문제였다.

1. **csproj 배포 트림 경로 오류** — RID 지정 publish(`-r win-x64`)에서 네이티브 DLL이 `runtimes\` 하위가 아니라 출력 루트로 평탄화되는데, 트림 타깃이 옛 경로만 지워 ffmpeg 25.2MB가 항상 남아있었다. 재배포로 375MB 확인.
2. **이미지 팔레트화** — RGBA PNG 1,642장 중 1,638장을 손실 감지 게이트(RGB 평균오차 ≤4.0, 알파 평균오차 ≤3.0 — 실측 300장 표본으로 보정)를 통과한 것만 팔레트 PNG로 전환. 144MB → 70MB. 게이트를 넘은 4장은 그룹 아트/복잡한 이펙트로, 실제로 원본 유지가 맞는 판단이었음을 육안으로 확인.
3. **RecognitionReferences 델타 편입** — `DataUpdateService`의 이미지 적용 로직을 `ApplyFileChanges` 공유 코어로 일반화해 인식 학습 참조 이미지에도 재사용. 중첩 경로용 zip-slip 방어(`IsSafeRelativePath`)를 새로 추가하고 5개 시나리오(정상 경로/경로 탈출/절대경로/누락 항목/구버전 호환)로 실제 검증.
4. **PowerShell 5.1의 `@(List<object>)` 예외** — 이 환경의 Windows PowerShell 빌드(5.1.26100.9168)에서 제네릭 리스트를 `@()`로 감싸면 무조건 "인수 형식이 일치하지 않습니다" 예외가 난다. `create_data_update.ps1`·`accept_data_baseline.ps1`의 기존 코드 전부가 이 패턴이었다 — 캐릭터나 이미지 변경이 하나라도 있으면 원래부터 스크립트가 끝까지 실행된 적이 없었다는 뜻. `.ToArray()`로 교체.
5. **DateTimeOffset 직렬화 형식 불일치** — PowerShell `ConvertTo-Json`이 `[DateTimeOffset]`을 레거시 `/Date(ms)/` 형식으로 쓰는데, C#의 `System.Text.Json`은 이걸 못 읽는다. `manifest.json`의 `CreatedAt`이 항상 이 형식이었으므로 **PS1이 만든 델타 zip은 단 한 번도 C# 앱에서 파싱된 적이 없었다.** ISO-8601(`.ToString("o")`)로 교체.
6. **zip 항목 경로 구분자 불일치** — Windows PowerShell 5.1의 `Compress-Archive`가 중첩 폴더 항목 이름에 `\`를 그대로 쓴다(ZIP 규격은 `/`). .NET `ZipArchive.GetEntry`는 정확히 일치하는 이름만 찾아 실패한다. 어떤 도구로 zip을 만들든 구분자와 무관하게 찾는 `FindEntry` 헬퍼로 교체 — 이미지·참조 양쪽에 적용되는 공통 지점이라 한 곳만 고치면 됐다.

6건 모두 실제 `create_data_update.ps1` 실행 → 생성된 진짜 zip을 C# `InspectPackage`로 파싱하는 end-to-end 테스트로 재현·수정·재검증했다. 테스트에 쓴 합성 참조 파일은 정리 완료, `Data/RecognitionReferences`는 원본 36개 파일 그대로다.

---

## 6. 결정 사항

구현 착수 전에 답이 필요했던 지점들. 2026-08-20 세션에서 전부 확정.

### D-1 — 앱 자체 업데이트를 어떻게 구현할까 → **Velopack**

- **기성 프레임워크(Velopack 등)** — GitHub Releases를 그대로 델타 소스로 쓰는 .NET 데스크톱 업데이트 라이브러리. 검증된 파일 교체·재시작 로직을 재사용.
- **자체 Updater.exe** — 배포 자산에 작은 헬퍼 exe를 포함해 종료 대기 → 파일 교체 → 재실행만 직접 구현. 의존성 추가 없음.

**결정**: Velopack. 프로세스 자기 자신을 교체하는 로직은 실패 시나리오(교체 중 정전, 권한 문제)가 많아 직접 구현의 위험 대비 이득이 낮다. 단, 이 선택은 배포 모델을 지금의 **포터블(폴더 그대로 실행)**에서 **설치형**(`%LocalAppData%`에 설치, 시작메뉴 바로가기, 게임/Discord류처럼 백그라운드 자동 업데이트)으로 바꾼다는 뜻이다 — 의도적으로 감수하기로 함.

### D-2 — 코드 서명 인증서를 구매할까 → **미구매**

- **구매** — 다운로드·자동 업데이트 시 SmartScreen 경고가 없다. 연 단위 비용 발생.
- **미구매** — 다운로드 페이지에 "추가 정보 → 실행" 우회 안내를 넣는다. 자동 업데이트가 받은 파일도 매번 이 경고를 유발할 수 있다.

**결정**: 미구매. 사용자가 직접 승인(추가 정보 → 실행)하면 우회 가능한 수준이라 초기 규모에서는 비용 대비 실익이 낮음. 사용자 규모가 커지면 재검토(Azure Trusted Signing이 저비용 대안).

### D-3 — 델타 체인을 언제 스냅샷으로 접을까 → **5개 데이터 버전마다**

- **버전 수 기준(채택)** — 5개 데이터 버전마다 전체 스냅샷 릴리스.
- **기간 기준** — 예: 분기마다.

**결정**: 버전 수 기준, 5개마다. 실제 데이터 업데이트 빈도가 아직 없어 근거 있는 최적값은 아니지만 초기값으로 채택 — 릴리스 몇 회 겪어본 뒤 숫자만 조정하면 되는 구조라 지금 정밀하게 맞출 필요 없음.

### D-4 — 버전 매니페스트를 어디에 둘까 → **리포 내 JSON**

- **리포 내 JSON(채택)** — `main` 브랜치의 raw 파일(예: `latest.json`)을 앱과 사이트가 함께 조회. 캐시하기 쉽고 GitHub CDN을 그대로 씀.
- **GitHub Releases API** — 태그/자산 메타데이터를 릴리스 API로 직접 조회. 별도 파일 관리가 없음.

**결정**: 리포 내 JSON. 자산 SHA256(FR-4) 같은 부가 정보를 자유롭게 넣을 수 있고 릴리스 API 레이트리밋에서 자유롭다.

---

## 7. 이번 계획에서 다루지 않는 것

- 웹 브라우저 배포(Supabase/서버 검색) — 별도로 검토했으나 스크린샷 인식(OpenCV)과 138MB 이미지 자산 때문에 이번 방향(데스크톱 유지)이 더 적합하다고 판단해 보류.
- 덱 스크린샷 인식 기능 변경 — 이번 계획은 배포 방식만 바꾼다. OpenCvSharp·인식 로직은 그대로 유지되며, 코드 변경 없이 자동 업데이트 대상에 포함된다.
- 캐릭터 편집·웹 반자동 등록 등 관리자 도구의 재작성 — 그대로 데스크톱 앱에 남는다.

---

## 8. 구현 진행 상황 (2026-08-20)

§6 결정 사항을 실제로 구현하고, 파이프라인 전체를 실행해 검증했다.

- **git/GitHub 저장소 신설·연결 완료** — 공개 저장소 `sksmsaidkeu/Kotodaman_Simulator`, `main` 브랜치에 소스 푸시 완료. `.gitignore`로 `bin/`·`obj/`·`Publish/`·`Releases/`·`Data/CharacterImages/`(70MB, PRD 방침대로 Release 자산으로만 배포) 제외.
- **Velopack 코드 통합 완료** — `Velopack 1.2.0` NuGet 추가, WPF 자동 `Main()` 생성을 끄고(`App.xaml` 빌드 액션 `ApplicationDefinition`→`Page`, `StartupObject` 지정) `App.xaml.cs`에 커스텀 `Main()`으로 `VelopackApp.Build().Run()`을 최우선 실행. `OnStartup`에서 `MainWindow.Show()` 직후 백그라운드로 `UpdateManager`(GithubSource, 공개 저장소라 토큰 불필요) 체크 → 다운로드 → `ApplyUpdatesAndRestart` 흐름 추가. 실패해도 앱 사용에는 지장 없도록 전부 try/catch로 감쌈.
- **릴리스 스크립트 작성 완료** — `publish_velopack_release.ps1`/`.bat`: 데이터 baseline 검증 → self-contained win-x64 publish → `vpk pack`(패키지 id `KotodamanWordFinder`, 채널 `win`, `--noPortable`) → `vpk upload github --publish`(토큰은 `gh auth token`으로 획득) → `latest.json` 생성(버전·태그·릴리스 URL·Setup.exe 다운로드 URL·SHA256·발행시각) → 커밋·푸시까지 자동.
- **파이프라인 실전 검증 완료(draft)** — 위 스크립트와 같은 단계를 `--publish` 없이 실제로 실행해 GitHub에 draft 릴리스(태그 `v1.25.1`, 제목 "코토테스터 v1.25.1")를 만들었다. `nupkg`·`Setup.exe`·`RELEASES`·`releases.win.json` 4개 자산 업로드 확인, `gh release view`로 한글 제목이 정상 저장된 것도 확인(콘솔 출력 깨짐은 로컬 터미널 인코딩 문제일 뿐 실제 데이터는 정상). Draft 상태라 아직 비공개.
- **이번 세션에 새로 발견·수정한 버그**: `publish_velopack_release.ps1`에 한글 문자열(`--packTitle` 등)이 있는데 UTF-8 BOM이 없으면 Windows PowerShell 5.1이 시스템 코드페이지(CP949로 추정)로 잘못 해석해 따옴표가 깨지고 파싱이 실패했다. 실제 `powershell.exe -File` 실행 경로로 재현한 뒤 UTF-8 BOM을 추가해 수정. (참고로 기존 `create_data_update.ps1` 등은 한글을 포함하고도 BOM 없이 정상 파싱됨 — 오역 시 우연히 따옴표 문자가 생기는지 여부에 좌우되는 것으로 보이며, 일반화하려면 한글 포함 `.ps1`은 항상 BOM을 붙이는 편이 안전하다.)

**다음 단계**: FR-1 다운로드 사이트(Vercel) — 정적 페이지 코드까지는 이번 세션에 준비, 실제 Vercel 배포는 사용자가 계정 연결 후 직접. 이후 draft 릴리스를 언제 `--publish`할지는 다운로드 사이트 준비 상태를 보고 사용자가 결정.

---

*이 문서는 실제 코드·빌드·실행 결과를 근거로 작성했다. §5의 수치와 버그는 이번 세션에서 직접 재현·수정·재검증했고, 추정치로 남겨둔 항목(코드 서명 비용, GitHub 무료 한도 등)은 실행 전 별도 확인이 필요하다.*
